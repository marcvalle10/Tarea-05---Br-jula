import 'package:flutter/material.dart';

// Paleta centralizada del estilo visual.
class AppColors {
  // Fondo / madera oscura (viñeta)
  static const wood = Color(0xFF1A0F0A);

  // Tinta (un poco más oscura para mejor contraste sobre pergamino)
  static const ink = Color(0xFF2F2119);

  // Bronce envejecido
  static const bronze = Color(0xFFA68966);

  // Arena / brillo suave (neón arena)
  static const glowSand = Color(0xFFE7C07A);

  // Pergamino
  static const parchment = Color(0xFFF4EAD5);
  static const parchment2 = Color(0xFFFDF9F0);

  // Borde (≈36% de opacidad de ink)
  static const border = Color(0x5C2F2119);
}
